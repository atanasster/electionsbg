rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the server Admin SDK may access usage/budget records.
    match /{document=**} { allow read, write: if false; }
  }
}
