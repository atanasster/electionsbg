rules_version = '2';

// Firestore is written ONLY by the Admin SDK inside the `scenarios` cloud
// function (functions/index.js) — no client SDK anywhere. Deny everything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
