{
  "name": "naiasno-news-evals-functions",
  "description": "Isolated public news-evaluation boundary for electionsbg-news",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "node scripts/sync-eval-contract.mjs && tsc -p tsconfig.json && node scripts/sync-eval-contract.mjs --runtime",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
