# Repository instructions

`CLAUDE.md` is the canonical repository guide. Before doing any work in this
repository, read it completely—continuing in chunks until EOF—and follow it.

Codex-specific adaptations:

- Interpret references to Claude Code as Codex where applicable.
- For project skills, use `.agents/skills/` instead of `.claude/skills/`.

Keep shared repository guidance in `CLAUDE.md`; keep this file as a router only.
